// Shared app configuration constants.
// Re-exports the canonical API base URL so lib/ files can import this
// without depending on pages/viewers/config.dart.

export 'api_config.dart' show apiUri, defaultApiBaseUrl;
